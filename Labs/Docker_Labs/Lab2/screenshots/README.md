# Lab 2 screenshots

PNG evidence for the Docker Iris lab (build, `docker ps`, `curl`, browser).

The full write-up, embedded copies of these images, and run instructions are in the parent **[`../README.md`](../README.md)** (see **Section 4 — Screenshots**).
